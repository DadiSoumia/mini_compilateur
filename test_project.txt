BeginProject M1_iv_2526;

Setup :

    define x | c | H | i | j: integer;
    define y : float;

    
    define compteur : integer = 0;
    define moyenne : float = 5.34;
    define mo: float = (-456.04);
    define a: float = (+6.0408);
    define b: float = 9.03;
   
    define B  : [integer;10] ; 
    define tab : [integer;20] ;

   
    const pi : float = 3.14159;
    const max : integer = 100;


Run :
{
  
    %% Affectation avec expression arithmétique
    a <- x + 5;
    b <- a * 2;
    c <-2;
    B[5]  <- (H+ 2) +  (5  + (-3)) ;

    %% Affectation tableau
    tab[0] <- 42;
    tab[1] <- x + 1;
    tab[c] <- tab[c] + tab[c] ;

     
    %% Affectation simple
    x <- 10;
    y <- 3.14;

    %% Condition if-else
    if (x > 5) then:
    {
        y <- 6;
    } else {
        a <- x / 9;
    } endIf;

    %% Condition if sans else
    if (x == 10) then:
    {
        compteur <- compteur + 1;
    } endIf;

     %% Condition if simbriquée
    if (x == 10) then:
    {
        compteur <- compteur + 1;
          if (x == 10) then:
          {
           compteur <- compteur + 1;
          } endIf;
           if (x > 5) then:
             {
              a <- x + 1;
             }
             else {
               a <- x - 1;
                } endIf;
    } endIf;


    %% Boucle while
    loop while (compteur < max)
    {
        compteur <- compteur + 1;
   
    } endloop;

    %% Boucles while imbriquées
    
    loop while (b < 5)
    {
        j <- 0;

        loop while (c < 3)
        {
            
            c <- c + 1;
        } endloop;

        b <- b + 1;
    } endloop;
    


    %% Boucle for
    for i in 1 to 10 {
         a <- x - 1;
        tab[i] <- i * 2 ;
    } endfor;
   
        %% Boucles for imbriquées
    for i in 1 to 5 {
        
        for j in 1 to 3 {
            tab[i] <- i * j;
        } endfor;

    } endfor;

   %% for avec while interne
for i in 1 to 4 {

  

    loop while (j < 2)
    {
        tab[i] <- i + j;
        j <- j + 1;
    } endloop;

} endfor;


    %% Expressions logiques
    if (((a > 0) AND (b < 100)) OR ((a > 0) AND (b < 100))) then:
    {
        c <- 1;
    } else {
        c <- 0;
    } endIf;

    if ((a == 0) OR (b == 0)) then:
    {
        moyenne <- 0.0;
    } endIf;

    if( NON(x == 0)) then:
    {
        moyenne <- y / x;
    } endIf;

    
   

    %% I/O
    input(x);
    output("La valeur de x est : ", x);
    output("Résultat : ", moyenne);


}

EndProject ;